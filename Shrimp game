local WindUI = loadstring(game:HttpGet("https://tree-hub.vercel.app/api/UI/WindUI"))()
local Window = WindUI:CreateWindow({
    Title = "🐙 | Shrimp Game", -- UI Title
    Icon = "https://images.app.goo.gl/hxvULNhzXNRjkS968", -- Url or rbxassetid or lucide
    Author = "Starless", -- Author & Creator
    Folder = "CloudHub", -- Folder name for saving data (And key)
    Size = UDim2.fromOffset(390, 390), -- UI Size
    KeySystem = { -- Creates key system
        Key = "StarLessScriptsOficial", -- key
        Note = "Join at the discord for the key!!", -- Note
        URL = "https://workink.net/1XU2/m69kner6", -- URL To get key (example: Discord)
        SaveKey = true, -- Saves the key in the folder specified above
    }, 
    Transparent = true,-- UI Transparency
    Theme = "Dark", -- UI Theme
    SideBarWidth = 170, -- UI Sidebar Width (number)
    HasOutline = true, -- Adds Oultines to the window
})
-- Play a sound when the UI opens
local soundService = game:GetService("SoundService")
local soundId = 8503531171 -- Replace with your sound ID
local sound = Instance.new("Sound", soundService)
sound.SoundId = "rbxassetid://" .. soundId
if sound.SoundId ~= "rbxassetid://" then
    sound:Play()
end
local Notification = WindUI:Notify({
    Title = "🤖 | StarBot Says :",
    Content = "✅️ | key validates successfully",
    Duration = 2.5,
})
local Notification = WindUI:Notify({
    Title = "🤖 | StarBot Says : ",
    Content = "👋 | Welcome",
    Duration = 2.5,
})
local Tab = Window:Tab({
    Title = "| Main",
    Icon = "settings-2", -- lucide or rbxassetid
})
local Button = Tab:Button({
    Title = "🔦 | Red Light, Green Light",
    Desc = "Auto Win the Red Light, Green Light Use when the game starts!!",
    Callback = function()
local Notification = WindUI:Notify({
    Title = "🤖 | StarBot Says : ",
    Content = "Clicked at the button!",
    Duration = 1.5,
})
local soundService = game:GetService("SoundService")
local soundId = 5914602124 -- Replace with your sound ID
local sound = Instance.new("Sound", soundService)
sound.SoundId = "rbxassetid://" .. soundId
if sound.SoundId ~= "rbxassetid://" then
    sound:Play()
end
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(160.602524, 3.5, 708.78717, -0.999794662, 2.62921462e-09, 0.0202631913, 2.62862199e-09, 1, -5.58844741e-11, -0.0202631913, -2.60872725e-12, -0.999794662)
        print("Clicked!")
    end
})
local Toggle = Tab:Toggle({
    Title = "🎠 | Mingle",
    Desc = "Auto Enter a Opened door",
    Value = true,
    Callback = function(state)
local Notification = WindUI:Notify({
    Title = "🤖 | StarBot Says : ",
    Content = "Clicked at the button!",
    Duration = 1.5,
})
local soundService = game:GetService("SoundService")
local soundId = 5914602124 -- Replace with your sound ID
local sound = Instance.new("Sound", soundService)
sound.SoundId = "rbxassetid://" .. soundId
if sound.SoundId ~= "rbxassetid://" then
    sound:Play()
end
        print(state)
    end,
})
local Tab = Window:Tab({
    Title = "Player Config.",
    Icon = "user-round-cog", -- lucide or rbxassetid
})
local Button = Tab:Button({
    Title = "👨‍💼 | Be a Guard",
    Desc = "Its free permanent",
    Callback = function()
local args = {
    [1] = "GuardPerm"
}

game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Functions"):WaitForChild("JoinRole"):InvokeServer(unpack(args))
local soundService = game:GetService("SoundService")
local soundId = 5914602124 -- Replace with your sound ID
local sound = Instance.new("Sound", soundService)
sound.SoundId = "rbxassetid://" .. soundId
if sound.SoundId ~= "rbxassetid://" then
    sound:Play()
end
local Notification = WindUI:Notify({
    Title = "🤖 | StarBot Says : ",
    Content = "Clicked at the button!",
    Duration = 1.5,
})
        print("Clicked!")
    end,
})
